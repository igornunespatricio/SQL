-- CRUD: operations in databases. They mean Create, Read, Update and Delete operations.

-- CREATE DATABASE: intuitive, create database
CREATE DATABASE teste;

-- DROP DATABASE: also intuitive, delete the database
DROP DATABASE teste;

-- Can only delete databases that are not connected. Not connetect databases appears differently in the Object Explorer. To disconnect, right click and discoonect it. then it will be able to delete it.

CREATE DATABASE hashtag;

-- after creating a database, to create tables in it, it is necessary to create another Query Tool with the new database selected.